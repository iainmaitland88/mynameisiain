{
  "name": "mynameisiain",
  "version": "0.0.1",
  "private": true,
  "description": "A stupid monorepo for trying stupid things",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean",
    "typecheck": "tsc -b",
    "typecheck:watch": "tsc -b --watch"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=8"
  },
  "packageManager": "pnpm@8.15.0"
}
