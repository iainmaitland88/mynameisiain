{
  "name": "@mynameisiain/zoo",
  "version": "0.0.5",
  "description": "Zoo management system",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "test": "echo \"No tests yet\"",
    "lint": "echo \"No linter configured yet\"",
    "release": "pnpm clean && pnpm build && pnpm publish --access public --no-git-checks"
  },
  "dependencies": {
    "@mynameisiain/animals": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
