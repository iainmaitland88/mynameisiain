{
  "name": "@mynameisiain/animals",
  "version": "0.0.1",
  "description": "Animal definitions",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "clean": "rm -rf dist",
    "test": "echo \"No tests yet\"",
    "lint": "echo \"No linter configured yet\""
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
